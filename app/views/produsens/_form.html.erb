<%= form_with(model: produsen, local: true) do |form| %>
  <% if produsen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produsen.errors.count, "error") %> prohibited this produsen from being saved:</h2>

      <ul>
      <% produsen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>